#ifndef _FXCG_MINICOMPAT
# error Included without minicompat. Use <fxcg/display.h>.
#else

#ifndef __fxCG_DISPLAY_HPP__
#define __fxCG_DISPLAY_HPP__
/* Many implementations missing, so this is really barren. */

// People actually use this just to get display syscalls..
#include <display_syscalls.h>

#endif
